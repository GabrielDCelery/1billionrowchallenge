{
    "extends": "@tsconfig/node22/tsconfig.json",
    "compilerOptions": {
        "outDir": "dist"
    },
    // need to declare the worker separately otherwise will get runtime error
    "files": ["src/main.ts", "src/aggregator/worker/run.ts"]
}
