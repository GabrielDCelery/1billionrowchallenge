{
    "name": "1billionrowchallenge",
    "version": "1.0.0",
    "main": "dist/main.js",
    "scripts": {
        "build:prepare": "rm -Rf dist && mkdir dist",
        "build:transpile": "tsc",
        "build": "npm run build:prepare && npm run build:transpile",
        "test": "node --import tsx --test ./src/**/*.test.ts"
    },
    "author": "",
    "license": "ISC",
    "description": "",
    "devDependencies": {
        "@types/node": "^22.19.11",
        "assert": "^2.1.0",
        "prettier": "3.3.2",
        "tsx": "^4.16.2",
        "typescript": "^5.5.3"
    },
    "dependencies": {
        "winston": "^3.13.0",
        "zod": "^3.23.8"
    }
}
